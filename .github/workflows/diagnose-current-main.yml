name: Diagnose current CGV Exams release

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate Apps Script syntax
        run: |
          cp google-apps-script/Code.gs /tmp/cgv-code.js
          node --check /tmp/cgv-code.js

      - name: Build static site
        run: npm run build:github-pages
        env:
          NEXT_PUBLIC_GOOGLE_APPS_SCRIPT_URL: https://script.google.com/macros/s/AKfycbz0_-mm0ya8mmBf-mCmFGQhRbR4h2GCVJjwtq3iGUTKZUDP8l5p89ahnSyP41rppA78lQ/exec

      - name: Run complete regression suite
        id: tests
        continue-on-error: true
        run: |
          mkdir -p audit
          set +e
          node --test tests/*.test.mjs 2>&1 | tee audit/test-output.log
          code=${PIPESTATUS[0]}
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Verify Pages artifact
        id: artifact
        continue-on-error: true
        run: |
          set +e
          test -d dist/client && test -f dist/client/index.html
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: current-actions-diagnostics
          path: audit/test-output.log
          if-no-files-found: error

      - name: Enforce results
        if: always()
        run: |
          test "${{ steps.tests.outputs.exit_code }}" = "0"
          test "${{ steps.artifact.outputs.exit_code }}" = "0"
